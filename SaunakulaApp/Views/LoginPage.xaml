<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SaunakulaApp.Views.LoginPage"
    Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="28,60,28,28" Spacing="0">

            <!-- Logo / Title -->
            <VerticalStackLayout Spacing="8" Margin="0,0,0,48">
                <Label Text="&#127794;"
                       FontSize="52"
                       HorizontalOptions="Center"/>
                <Label Text="Saunak&#252;la"
                       FontSize="28" FontAttributes="Bold"
                       TextColor="{StaticResource Primary}"
                       HorizontalOptions="Center"/>
                <Label Text="Tere tulemast tagasi!"
                       Style="{StaticResource MutedLabel}"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>

            <!-- Email -->
            <Label Text="E-post"
                   FontSize="14" FontAttributes="Bold"
                   TextColor="{StaticResource TextPrimary}"
                   Margin="0,0,0,6"/>
            <Frame CornerRadius="14"
                   BackgroundColor="{StaticResource MutedBackground}"
                   BorderColor="Transparent"
                   Padding="16,0"
                   HeightRequest="52"
                   HasShadow="False"
                   Margin="0,0,0,16">
                <Entry Text="{Binding Email}"
                       Placeholder="sinu@email.ee"
                       Keyboard="Email"
                       BackgroundColor="Transparent"
                       HeightRequest="52"/>
            </Frame>

            <!-- Password -->
            <Label Text="Parool"
                   FontSize="14" FontAttributes="Bold"
                   TextColor="{StaticResource TextPrimary}"
                   Margin="0,0,0,6"/>
            <Frame CornerRadius="14"
                   BackgroundColor="{StaticResource MutedBackground}"
                   BorderColor="Transparent"
                   Padding="16,0"
                   HeightRequest="52"
                   HasShadow="False"
                   Margin="0,0,0,8">
                <Entry Text="{Binding Password}"
                       Placeholder="&#8226;&#8226;&#8226;&#8226;&#8226;&#8226;&#8226;&#8226;"
                       IsPassword="True"
                       BackgroundColor="Transparent"
                       HeightRequest="52"/>
            </Frame>

            <!-- Error message -->
            <Label Text="{Binding ErrorMessage}"
                   TextColor="{StaticResource Destructive}"
                   FontSize="13"
                   IsVisible="{Binding HasError}"
                   Margin="0,0,0,16"/>

            <!-- Login Button -->
            <Button Text="Logi sisse"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding LoginCommand}"
                    Margin="0,16,0,0"/>

            <!-- Divider -->
            <Grid ColumnDefinitions="*,Auto,*"
                  ColumnSpacing="12"
                  Margin="0,24,0,24">
                <BoxView BackgroundColor="{StaticResource Border}"
                         HeightRequest="1"
                         VerticalOptions="Center"/>
                <Label Grid.Column="1"
                       Text="v&#245;i"
                       Style="{StaticResource MutedLabel}"/>
                <BoxView Grid.Column="2"
                         BackgroundColor="{StaticResource Border}"
                         HeightRequest="1"
                         VerticalOptions="Center"/>
            </Grid>

            <!-- Register link -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="4">
                <Label Text="Pole kontot?"
                       Style="{StaticResource MutedLabel}"/>
                <Label Text="Registreeru"
                       FontSize="14" FontAttributes="Bold"
                       TextColor="{StaticResource Primary}">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToRegisterCommand}"/>
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
